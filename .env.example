# Copy this file to .env and paste your Anthropic API key after the equals sign.
# Get a key at https://console.anthropic.com/ -- it is the lantern's flame, the
# only secret here, and it never leaves the server (the browser talks only to
# your local server).
ANTHROPIC_API_KEY=
