# GetThrough runs on exactly four packages, on purpose (see SEED.md).
# FastAPI + uvicorn serve one small backend file; anthropic talks to the
# model; python-dotenv reads the API key from .env. Nothing else.
fastapi
uvicorn
anthropic
python-dotenv
